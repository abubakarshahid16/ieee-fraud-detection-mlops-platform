apiVersion: v1
kind: PersistentVolume
metadata:
  name: fraud-mlops-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/fraud-mlops
